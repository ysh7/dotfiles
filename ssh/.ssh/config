# add key to agent
AddKeysToAgent yes

# use connection multiplexing
ControlMaster auto
ControlPath ~/.ssh/control/%r@%h:%p
ControlPersist 1800

# timeout to avoid idle disconnection
ServerAliveInterval 20
ServerAliveCountMax 10
